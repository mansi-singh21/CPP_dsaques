#include <iostream>
#include <string>
using namespace std;
int main(){
    string s;
    cout<<"Enter a string: ";
    cin>>s;
    int st=0;
    int end=s.length();
    while(st<(end/2)){
        swap(s[st],s[end-st-1]);
        st++;
    }
    cout<<"reversed string: "<<s;
}
